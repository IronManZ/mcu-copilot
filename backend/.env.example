# MCU-Copilot 环境变量配置模板
# 复制此文件为 .env 并填入真实的API密钥

# LLM API Keys
QIANWEN_APIKEY=your-qianwen-api-key-here
GEMINI_APIKEY=your-gemini-api-key-here

# JWT Authentication
JWT_SECRET_KEY=your-super-secure-secret-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_HOURS=24

# API Authentication (for seed users)
# Generate a secure random token for production
API_TOKEN=your-secure-random-token-here

# Server Configuration (optional)
HOST=0.0.0.0
PORT=8000
DEBUG=false

# CORS Configuration
# For testing phase with curl access only, you can use one of these options:
# Option 1: Allow all origins (testing phase)
ALLOWED_ORIGINS=*
# Option 2: Allow specific server IPs (more secure)
# ALLOWED_ORIGINS=http://8.219.74.61:8000,http://localhost:8000
# Option 3: Leave empty to use default development settings (recommended for curl-only testing)
# ALLOWED_ORIGINS=